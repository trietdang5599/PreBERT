# Install PyTorch appropriate for your CUDA/CPU platform first when necessary.
torch>=2.1
transformers>=4.45,<5
accelerate>=1.0
safetensors>=0.4
numpy>=1.24
scikit-learn>=1.3

# Optional, required only by --use-4bit on NVIDIA CUDA:
# bitsandbytes>=0.43
